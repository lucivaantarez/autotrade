import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';

const source = readFileSync(new URL('./zekehub_winterhub.lua', import.meta.url), 'utf8');
for (const expected of ['getgenv().scriptkey = getgenv().scriptkey or ""', 'PASTE YOUR ZEKEHUB KEY', 'WINTERHUB_WEBHOOK_URL', 'task.spawn(send_trade_webhook, last_items)', 'AutoAcceptTrades = true', 'AutoLeaveAfterTrades = false', 'AccountManager = {', 'task.spawn', 'CONFIG.Poll = positive_number(CONFIG.Poll, 0.1)', 'CONFIG.StartupGrace = positive_number(CONFIG.StartupGrace, 45)', 'type(CONFIG.ForceSettings) == "table"', 'type(CONFIG.Webhook) == "table"', 'CONFIG.ForceSettings.TradeRequests == nil', 'startup_ready', 'has_seen_trade = true', 'pcall(write_status)', 'trade-state error', 'fatal_error', 'pcall(function() return game:HttpGet(ZEKEHUB_URL) end)', 'utility.WinterHub', 'ForceSettings', 'force_everyone("trade_requests")', 'force_everyone("give_item_requests")', '_winteraddons.json', 'status = "completed"', 'ts = os.time()', 'count = trade_count', 'items = last_items', 'mine.confirmed and partner.confirmed', 'return result']) assert(source.includes(expected));
console.log('ZekeHub WinterHub wrapper contract simulation passed.');
